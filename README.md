进行前端设计的改进：
1、采用苹果的极简设计。整体颜色为白色、淡灰色，字体为黑色，微软雅黑。
2、左侧为三行：第一行h1，内容为"密码强度评估"；第二行为密码输入框，要求对密码进行保护，输入时展示为星号，输入框右端有眼睛按钮，点击后可以显示密码内容。密码未输入时，为灰色提示内容，随机展示”放心，所有计算均处于本地“或”放心，开发者无法得到你的密码“等。输入光标为"_"，并持续闪烁。第一、二行的文字大小要求相同，粗体。第三行为<p>，显示随机的密码建议。
3、右侧为两个组件，正右方是一个Ω形状的进度环，环中央是密码强度（熵），当密码强度大于100时，环进度条为满，且环能根据密码强度变色。右上方是两个切换按钮，切换”默认“和”高级“，现只需要实现组件，不需要实际跳转。
将所有的css保存到单独app.css中。